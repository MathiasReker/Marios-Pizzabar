package java.com.app.model;

public class OrderParserModel {

/*  public String orderToString(OrderModel [] order){//TODO needs to be refactored

    String orderToString = "";

    for (int i = 0; i < orderLines.size(); i++) {
      orderToString +=  orderLines.get(i).orderLineToString();
    }

    orderToString +=";" + orderId + ";" + expectedPickUpTime + ";" + orderStatus;

    return orderToString;
  }*/
}
